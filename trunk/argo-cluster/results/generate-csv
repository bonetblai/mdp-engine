#!/bin/bash

domain=$1
problems=`../print-problems $domain`

rm -fr data.csv data
for p in $problems; do
    echo "Extracting: $p"
    ../extract-csv $domain $p
done

cat ../header.csv data/* > data.csv

